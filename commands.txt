Pthreads:

    Build:
        gcc 3way-pthread/pth-imp.c -o 3way-pthread/pth-imp

    Run:
        sbatch full-pth.sh

MPI:

    Build:
        mpicc ./3way-mpi/mpi-imp.c -o 3way-mpi/mpi-imp

    Run:
        sbatch full-mpi.sh

Open MPI:

    Build:
        mpicc ./3way-openmp/openmp-imp.c -lgomp -o 3way-openmp/openmp-imp

    Run:
        sbatch full-opmpi.sh
        
